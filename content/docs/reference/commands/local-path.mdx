---
title: local-path
description: Print the path to each target's local state when their active state path is given. If no targets are specified then print the path to the local state directory.
---

## Usage

```bash title="Shell"
punkt local-path [<options>] [<targets>]
```

## Examples

```bash
punkt local-path
punkt local-path ~/.audrey
```
